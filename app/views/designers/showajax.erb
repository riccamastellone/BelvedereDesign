<%=
    JSON.generate(
             "nome" => @designer.name,
             "descrizione" => nl2br(@designer.description).html_safe,
             "immagine" => "/data/" + @designer.image_url,
             "prodotti" => @prodottiContainer.as_json).html_safe
%>